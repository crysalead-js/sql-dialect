{
  "name": "sql-dialect",
  "version": "4.1.0",
  "description": "SQL Builder",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "http://github.com/crysalead-js/sql-dialect.git"
  },
  "keywords": [
    "sql"
  ],
  "dependencies": {
    "extend-merge": "^1.0.5",
    "string-placeholder": "^1.0.2"
  },
  "main": "src/index.js",
  "devDependencies": {
    "chai-kahlan": "^2.0.0",
    "coveralls": "^3.0.8",
    "jasmine-kahlan": "^1.0.2",
    "mocha": "^6.2.2",
    "nyc": "^14.1.1"
  },
  "scripts": {
    "coverage-report": "nyc --reporter=lcov mocha",
    "push-coverage": "cat ./coverage/lcov.info | coveralls",
    "cleanup": "rm -rf ./coverage",
    "test": "mocha"
  }
}
